name: Python linting
on:
  push:
    paths:
      - '**.py'
      - '.github/workflows/lint.yaml'
      - lib/galaxy/dependencies/**
      - mypy.ini
  pull_request:
    paths:
      - '**.py'
      - '.github/workflows/lint.yaml'
      - lib/galaxy/dependencies/**
      - mypy.ini
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.9', '3.13']
    env:
      LINT_PATH: 'lib/galaxy/dependencies/pinned-lint-requirements.txt'
      TYPE_PATH: 'lib/galaxy/dependencies/pinned-typecheck-requirements.txt'
      CORE_PATH: 'lib/galaxy/dependencies/pinned-requirements.txt'
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install uv
        uses: astral-sh/setup-uv@v7
      - name: Get full Python version
        id: full-python-version
        shell: bash
        run: echo "version=$(python -c 'import sys; print("-".join(str(v) for v in sys.version_info))')" >> $GITHUB_OUTPUT
      - name: Install tox
        run: uv tool install tox --with tox-uv
      - name: Run linting
        run: tox -e lint-ci
      - name: Run docstring linting
        run: tox -e lint_docstring_include_list
      - name: Run mypy checks
        run: tox -e mypy-ci
      # Ideally we would have an artifact for these but I don't see a way
      # to hack the actions to do that. -John
      - uses: psf/black@stable
        with:
          version: ">=25.1.0"
      - uses: jmchilton/isort-action@feat/output-file-support
        with:
          output-file: isort-report.txt
      - uses: actions/upload-artifact@v5
        if: failure()
        with:
          name: Mypy report (${{ matrix.python-version }})
          path: mypy-report.json
      - uses: actions/upload-artifact@v5
        if: failure()
        with:
          name: Ruff report (${{ matrix.python-version }})
          path: ruff-report.json
      - uses: actions/upload-artifact@v5
        if: failure()
        with:
          name: isort report (${{ matrix.python-version }})
          path: isort-report.txt
